reen": {
                "launchShowDuration": 0,
                "launchAutoHide": true,
                "backgroundColor": "#050a16",
                "splashFullScreen": true,
                "splashImmersive": true
              },
              "StatusBar": {
                "style": "DARK",
                "backgroundColor": "#050a16"
              }
            }
          }
          EOF

      - name: Prepare www folder
        run: |
          rm -rf www
          mkdir -p www
          cp index.html manifest.json service-worker.js sw.js style.css app.js favicon.ico favicon.png logo.png www/ 2>/dev/null || true
          cp -r css js assets auth users www/ 2>/dev/null || true
          cp -r .well-known www/ 2>/dev/null || true
          find . -maxdepth 1 -name "*.html" ! -name "index.html" -exec cp {} www/ \; 2>/dev/null || true
          ls -la www/

      - name: Add Android platform
        run: |
          rm -rf android
          npx cap add android

      - name: Add Native Google Sign-In
        run: |
          mkdir -p android/app/src/main/java/ir/nightcast/app

          cat > android/app/src/main/java/ir/nightcast/app/MainActivity.java <<'EOF'
          package ir.nightcast.app;

          import android.os.Bundle;

          import com.getcapacitor.BridgeActivity;

          public class MainActivity extends BridgeActivity {

              @Override
              public void onCreate(Bundle savedInstanceState) {

                  registerPlugin(NightCastGoogleAuth.class);

                  super.onCreate(savedInstanceState);
              }
          }
          EOF

          cat > android/app/src/main/java/ir/nightcast/app/NightCastGoogleAuth.java <<'EOF'
          package ir.nightcast.app;

          import android.app.Activity;

          import androidx.annotation.NonNull;
          import androidx.credentials.Credential;
          import androidx.credentials.CredentialManager;
          import androidx.credentials.CustomCredential;
          import androidx.credentials.GetCredentialRequest;
          import androidx.credentials.GetCredentialResponse;
          import androidx.credentials.CredentialManagerCallback;
          import androidx.credentials.exceptions.GetCredentialCancellationException;
          import androidx.credentials.exceptions.GetCredentialException;
          import androidx.credentials.exceptions.GetCredentialCustomException;
          import androidx.credentials.exceptions.NoCredentialException;

          import com.getcapacitor.JSObject;
          import com.getcapacitor.Plugin;
          import com.getcapacitor.PluginCall;
          import com.getcapacitor.annotation.CapacitorPlugin;

          import com.google.android.libraries.identity.googleid.GetSignInWithGoogleOption;
          import com.google.android.libraries.identity.googleid.GoogleIdTokenCredential;
          import com.google.android.libraries.identity.googleid.GoogleIdTokenParsingException;

          @CapacitorPlugin(name = "NightCastGoogleAuth")
          public class NightCastGoogleAuth extends Plugin {

              private static final String WEB_CLIENT_ID =
                      "242292157493-km4c11qgkf0lr3e6pv9paspkn95jbf3a.apps.googleusercontent.com";

              private CredentialManager credentialManager;

              private boolean signInInProgress = false;

              @Override
              public void load() {
                  super.load();

                  credentialManager =
                          CredentialManager.create(getContext());
              }

              @com.getcapacitor.PluginMethod
              public void signIn(PluginCall call) {

                  Activity activity = getActivity();

                  if (activity == null) {
                      call.reject(
                              "Android Activity is not available."
                      );
                      return;
                  }

                  if (signInInProgress) {
                      call.reject(
                              "Google sign-in is already in progress."
                      );
                      return;
                  }

                  signInInProgress = true;

                  final GetSignInWithGoogleOption googleOption;

                  try {

                      googleOption =
                              new GetSignInWithGoogleOption.Builder(
                                      WEB_CLIENT_ID
                              ).build();

                  } catch (Exception e) {

                      signInInProgress = false;

                      call.reject(
                              "Unable to initialize Google sign-in."
                      );

                      return;
                  }

                  final GetCredentialRequest request =
                          new GetCredentialRequest.Builder()
                                  .addCredentialOption(googleOption)
                                  .build();

                  credentialManager.getCredentialAsync(
                          activity,
                          request,
                          null,
                          activity.getMainExecutor(),
                          new CredentialManagerCallback<
                                  GetCredentialResponse,
                                  GetCredentialException>() {

                              @Override
                              public void onResult(
                                      @NonNull GetCredentialResponse result
                              ) {

                                  signInInProgress = false;

                                  handleCredential(
                                          call,
                                          result
                                  );
                              }

                              @Override
                              public void onError(
                                      @NonNull GetCredentialException error
                              ) {

                                  signInInProgress = false;

                                  if (error instanceof
                                          GetCredentialCancellationException) {

                                      call.reject(
                                              "Google sign-in was cancelled."
                                      );

                                      return;
                                  }

                                  if (error instanceof
                                          NoCredentialException) {

                                      call.reject(
                                              "No Google credential is available on this device."
                                      );

                                      return;
                                  }

                                  if (error instanceof
                                          GetCredentialCustomException) {

                                      call.reject(
                                              "Google Credential Manager could not complete the sign-in."
                                      );

                                      return;
                                  }

                                  call.reject(
                                          "Google sign-in failed."
                                  );
                              }
                          }
                  );
              }

              private void handleCredential(
                      PluginCall call,
                      GetCredentialResponse result
              ) {

                  try {

                      Credential credential =
                              result.getCredential();

                      if (!(credential instanceof CustomCredential)) {

                          call.reject(
                                  "Google did not return a Google ID credential."
                          );

                          return;
                      }

                      CustomCredential customCredential =
                              (CustomCredential) credential;

                      if (!GoogleIdTokenCredential
                              .TYPE_GOOGLE_ID_TOKEN_CREDENTIAL
                              .equals(customCredential.getType())) {

                          call.reject(
                                  "Unsupported Google credential type."
                          );

                          return;
                      }

                      GoogleIdTokenCredential googleCredential =
                              GoogleIdTokenCredential.createFrom(
                                      customCredential.getData()
                              );

                      String idToken =
                              googleCredential.getIdToken();

                      if (idToken == null || idToken.isEmpty()) {

                          call.reject(
                                  "Google did not return an ID token."
                          );

                          return;
                      }

                      JSObject response =
                              new JSObject();

                      response.put(
                              "idToken",
                              idToken
                      );

                      call.resolve(response);

                  } catch (
                          GoogleIdTokenParsingException e
                  ) {

                      call.reject(
                              "Google ID token could not be parsed."
                      );

                  } catch (Exception e) {

                      call.reject(
                              "Google credential processing failed."
                      );
                  }
              }
          }
          EOF
          python3 - <<'PY'
          from pathlib import Path

          gradle = Path("android/app/build.gradle")

          text = gradle.read_text()

          dependencies = """
              implementation "androidx.credentials:credentials:1.6.0"
              implementation "androidx.credentials:credentials-play-services-auth:1.6.0"
              implementation "com.google.android.libraries.identity.googleid:googleid:1.2.0"
          """

          if 'androidx.credentials:credentials:1.6.0' not in text:
              marker = "dependencies {"

              if marker not in text:
                  raise SystemExit(
                      "Could not find dependencies block in android/app/build.gradle"
                  )

              text = text.replace(
                  marker,
                  marker + "\n" + dependencies,
                  1
              )

              gradle.write_text(text)

              print("Google Native Sign-In dependencies added.")
          else:
              print("Google Native Sign-In dependencies already exist.")
          PY
      - name: Sync web assets
        run: npx cap sync android
        
      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build Debug APK
        run: |
          cd android
          ./gradlew assembleDebug --stacktrace

      - name: Decode Keystore
        continue-on-error: true
        run: |
          if [ -n "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" ]; then
            echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > nightcast-release.keystore
            echo "Keystore decoded successfully"
            ls -lh nightcast-release.keystore
          else
            echo "No keystore secret found"
          fi

      - name: Build Release APK
        run: |
          if [ -f "nightcast-release.keystore" ]; then
            echo "Building signed Release APK..."
            cd android
            ./gradlew assembleRelease \
              -Pandroid.injected.signing.store.file="$(pwd)/../nightcast-release.keystore" \
              -Pandroid.injected.signing.store.password="${{ secrets.KEYSTORE_PASSWORD }}" \
              -Pandroid.injected.signing.key.alias="${{ secrets.KEY_ALIAS }}" \
              -Pandroid.injected.signing.key.password="${{ secrets.KEY_PASSWORD }}" \
              --stacktrace
          else
            echo "Building unsigned Release APK..."
            cd android
            ./gradlew assembleRelease --stacktrace
          fi

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: NightCast-Debug-APK
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 30
          if-no-files-found: warn

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: NightCast-Release-APK
          path: android/app/build/outputs/apk/release/*.apk
          retention-days: 30
          if-no-files-found: warn

      - name: List built APKs
        run: |
          echo "=== Built APKs ==="
          find android/app/build/outputs/apk -name "*.apk" -exec ls -lh {} \; || true
